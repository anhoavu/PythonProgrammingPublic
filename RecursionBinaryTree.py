class BinaryTreeNode:
    def __init__(self, value = 0, left = None, right = None):
        self.value = value
        self.left = left
        self.right = right

def heightOfTree(root):
    # Compute the height of the binary tree with given root node
    # Note: the height is the length of the longest path from root to a leaf node
    return 0

def longestPathInTree(root):
    # Return a longest path in the binary tree (a list of BinaryTreeNode)
    # Hint: Change the heightOfTree appropriately
    return 0

def countNodes(root):
    # Count the number of nodes in the binary tree
    return 0

def findLeaves(root):
    # Compute the list containing the leaf nodes in the binary tree
    return None

def collectNodesByLevel(root):
    # Compute the list (of list) R where R[i] is the list containing nodes at level i
    return None
